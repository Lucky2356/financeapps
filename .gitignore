node_modules
.next
out
dist
coverage
.env
.env*.local
*.log
*.tsbuildinfo
*-qa.png
dev-server.*.log
prisma/migrations/dev.db
src-tauri/target
playwright-report
test-results
blob-report
.playwright
.playwright-mcp

# Tauri updater signing keys — must never be committed (store as GitHub secrets)
*.key
*.key.pub

# Android release signing — the keystore and its passwords stay on the owner's
# machine (and in GitHub secrets), never in the repository.
*.jks
*.keystore
keystore.properties
emu-*.png
shots/

# Screenshots produced by e2e/shots.spec.ts on demand
/shots/

# CodeGraph's local index — a cache of this checkout, rebuilt on demand.
.codegraph/
