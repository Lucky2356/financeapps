# Политика безопасности

## Поддерживаемые версии

Обновления безопасности выпускаются для последней версии. Используйте свежий релиз
с [страницы Releases](https://github.com/Lucky2356/financeapps/releases).

| Версия | Поддержка |
|---|---|
| Последний релиз | ✅ |
| Более старые | ❌ |

## Сообщить об уязвимости

**Пожалуйста, не сообщайте об уязвимостях через публичные GitHub Issues.**

Используйте приватный канал:

- **GitHub Security Advisories** — вкладка *Security → Report a vulnerability*
  ([создать приватный отчёт](https://github.com/Lucky2356/financeapps/security/advisories/new)).

В отчёте по возможности укажите:

- тип уязвимости и затронутый компонент (Windows / Android / зависимость);
- шаги воспроизведения или PoC;
- потенциальное влияние;
- предлагаемое исправление, если есть.

Мы постараемся подтвердить получение в течение нескольких дней и держать вас в курсе
хода исправления. Пожалуйста, дайте разумное время на выпуск патча до публичного
раскрытия (ответственное разглашение).

## Модель безопасности — кратко

- **Windows и Android:** данные лежат на устройстве (IndexedDB) и по умолчанию его не
  покидают. Общего облака у приложения нет: пока владелец не подключил службу, сервера
  в его жизни не существует, и приложение работает целиком местно.
- **Если служба подключена**, данные уезжают на неё **зашифрованными**, и ключа у неё
  нет. Шифрование стоит НИЖЕ отправки в том смысле, что отправляющий слой открытых
  данных не видит физически: в нём их нет. Служба хранит непрозрачные для неё куски и
  не может их прочитать — ни владелец службы, ни тот, кто получит к ней доступ.
- **Что служба всё же видит:** что учётная запись существует; имя входа; имена, которые
  владелец дал своим устройствам; размер каждого куска и время каждого изменения; адрес,
  с которого пришёл запрос. По времени и размеру можно судить о том, что человек вносит
  операции, но не о том, какие.
- **Перенос данных между устройствами** делает либо служба, либо сам владелец файлом
  резервной копии. Файл копии **не шифруется**, поэтому хранить его нужно так же
  бережно, как выписку из банка.
- **Секреты** (ключ подписи обновлений, Android-keystore и его пароли, API-ключи)
  **никогда** не коммитятся в репозиторий. API-ключ ИИ-провайдера хранится на устройстве
  без шифрования и **не попадает в резервную копию** — на новом компьютере его вводят
  заново.

## Что уходит наружу и когда

Приложение работает без сервера, но несколько вещей всё же покидают устройство. Ниже —
что именно, куда и по какому поводу.

| Что | Куда | Когда |
| --- | --- | --- |
| Тикеры бумаг из портфеля и списка наблюдения | `iss.moex.com`, `smart-lab.ru` | При обновлении котировок. По последовательности запросов третья сторона восстанавливает состав портфеля |
| Коды валют | `www.cbr.ru` | При обновлении курсов |
| Сводка по финансам: средние доход и расход, норма сбережений, названия и суммы топ-категорий | Выбранный ИИ-провайдер | Только при явном запросе разбора или совета. Отдельные операции не отправляются |
| **Описания** выбранных операций, как они записаны | Выбранный ИИ-провайдер | Только при пакетной категоризации и только после подтверждения в отдельном окне. Суммы и счета не отправляются |
| Версия приложения (в адресе запроса) | `github.com`, `api.github.com` | При проверке обновлений, раз в сутки |
| Зашифрованные куски данных и имена их ячеек | Служба, которую выбрал сам владелец | Только если он подключил службу. Ключа у неё нет; имена ячеек (`profileList`, `localFinanceState_…`) она видит, содержимое — нет |
| Имя входа — и больше ничего | Служба, выдавшая код связки | При наборе кода связки на втором устройстве. Пароля в коде нет: он не уезжает ни в коде, ни в картинке |

Отключение обновления котировок и ИИ-функций прекращает первые четыре обращения;
проверка обновлений отключается в настройках отдельно; служба перестаёт получать
что-либо, как только владелец отключит устройство от неё.

## Служба по умолчанию

Начиная с 1.44.0 в сборку зашит адрес службы, к которой приложение предлагает
подключиться, если человек не назвал своего. Это то, чем выполняется обещание
«скачал и работает»: иначе первым вопросом человеку, собравшемуся связать
телефон с компьютером, было бы «адрес вашей службы».

Что это меняет, прямо:

- **Ничего — пока человек не нажал «связать».** Приложение остаётся местным по
  умолчанию, и службы в его жизни не существует, пока он сам её не позовёт.
- Нажавший попадает на машину, которую держит владелец приложения. Данные
  лежат там зашифрованными, и ключа у службы нет. Но **существование учётной
  записи, её имя входа, размеры ячеек и время правок** видны тому, кто эту
  машину держит, — ровно то же, что и про любую другую службу в таблице выше.
- Путь «своя служба» никуда не делся: он перестал быть первым вопросом, но
  остался полноценным. Собирающие приложение себе задают свой адрес
  переменной `NEXT_PUBLIC_FINANCE_SERVER` на сборке; пустое значение убирает
  предложение совсем.

Кто не хочет, чтобы и эти следы видел кто-то посторонний, поднимает службу сам
(`server/README.md`) или не связывает устройства вовсе. Обоим этим ответам
приложение не мешает.

## Код связки

Восемь знаков, которые первое устройство показывает второму. Что о нём важно:

- **Пароля в нём нет и не будет.** Пароль — единственное, чем завёрнут ключ от
  данных; поехав в коде (а значит и в картинке QR, которую снимают из-за плеча
  или пересылают в мессенджере), он сделал бы бессмысленным всё шифрование
  разом. На втором устройстве пароль вводят руками.
- Код отдаёт **имя входа** и адрес службы. Имя входа не тайна — служба и так
  отвечает на `/auth/params`, заведено ли такое имя, — но и разбрасываться им
  незачем: код живёт пять минут, срабатывает один раз, лежит на службе хешем, а
  перебор ограничен двадцатью попытками в минуту с адреса.
- Кто перехватил код, узнаёт имя входа. Открыть данные он этим не может:
  нужен пароль, а пароль остаётся у человека.

### Картинка QR

Та же восьмёрка знаков плюс адрес службы — и больше ничего. Пароля в картинке
нет по той же причине, по какой его нет в коде, только здесь она весомее:
картинку снимают из-за плеча, пересылают и оставляют на экране.

Адрес в картинке — это то, за что приходится платить внимательностью.
Подсунутая чужая картинка уводит приложение на чужой адрес, и там у него можно
выпросить выведенный из пароля секрет входа. Поэтому:

- адрес принимается **только по https** — иначе и код, и секрет уехали бы
  открытым текстом;
- прежде чем спросить пароль, приложение **показывает**, к какой службе и под
  каким именем оно подключается, — тем же экраном, что и при наборе кода
  руками. Молча подключаться к тому, что принесла картинка, оно не станет.

Камеру приложение просит ровно для этого и больше ни для чего; в манифесте
Android она помечена необязательной, и без неё приложение работает полностью.

## Несколько людей на одном устройстве

У каждого человека на устройстве свои данные, свой пароль и своя связь со службой.
Разделение сделано приставкой в именах ключей, ниже шифрования и ниже отправки, и вот
что оно даёт и чего НЕ даёт.

**Даёт:** приложение, открытое на одного, не показывает и не стирает данные другого.
«Забыл и пароль, и код» стирает только своё. Подключение к службе у каждого своё, и
двое, попытавшись войти под одной учётной записью, получат отказ — иначе их данные
слились бы в одни молча.

**Не даёт:** защиты от соседа, у которого есть этот компьютер. Разделение честно ровно
настолько, насколько заперты данные:

- у человека **без пароля** ключ лежит в хранилище открытым — по замыслу, иначе
  работать без пароля было бы нельзя;
- то же у поставившего **«не спрашивать на этом устройстве»**: пометка и есть открыто
  лежащий ключ.

В обоих случаях сосед, взявший компьютер, откроет эти данные. Приложение говорит об
этом прямо на экране выбора человека, подписью на его плитке, а не мелким шрифтом.
Единственная настоящая защита от соседа — пароль, который он не знает.

**Список людей устройства** не шифруется: он про устройство, а не про чьи-то данные. В
нём лежат имена, которые люди придумали сами, и отпечаток от «адрес службы|имя входа» —
именно отпечаток, а не логин, чтобы сосед не прочитал чужое имя входа, открыв
хранилище.

## Почему в CSP остаётся `unsafe-inline`

`script-src` в `src-tauri/tauri.conf.json` разрешает встроенные скрипты: Next.js
раскладывает данные страницы по инлайновым `<script>`, содержимое которых меняется с
каждой сборкой. Убрать разрешение технически можно — блоков в собранной оболочке 104,
но уникальных всего 22, и список их хешей можно пересчитывать на сборке. Не сделано
сознательно: расхождение в один байт даёт белое окно вместо приложения, а проверить это
можно только установив собранный релиз.

Разрешение безвредно ровно до тех пор, пока чужой текст — описание операции, название
категории, ячейка CSV, ответ модели — не может стать разметкой. Сейчас такого пути нет:
React экранирует всё, что рендерит, а конструкции в обход React отсутствуют. Единственное
исключение — скрипт в `<head>`, проставляющий сохранённую плотность интерфейса до первой
отрисовки; он не принимает данных, а читает `localStorage` сам.

Это не обещание на словах: `tests/no-html-injection.test.ts` роняет сборку, если в
`app/`, `components/`, `hooks/`, `lib/`, `services/` или `types/` появится `innerHTML`,
`outerHTML`, `insertAdjacentHTML`, `document.write`, `eval`, `new Function` или `srcDoc`,
а также если `dangerouslySetInnerHTML` получит что-либо кроме заявленной константы — или
если в саму константу заведут подстановку.

## Что в зоне риска

- Содержимое файла резервной копии (полный срез финансов владельца; ключи в него не входят)
- Служба синхронизации: она не читает данные, но знает, кто, когда и сколько пишет, — и остаётся целью для того, кто хочет это знать
- Данные человека без пароля на общем компьютере: их открывает любой, кто до компьютера добрался
- Цепочка обновления Windows (подпись релизов) и подпись APK
- XSS и обход валидации (Zod) в клиентском коде
- Уязвимости в зависимостях (`npm audit`)

Спасибо, что помогаете сохранять проект и его пользователей в безопасности. 🔐
